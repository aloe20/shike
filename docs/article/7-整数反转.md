---
title: 7.整数反转 
date: 2021-04-07 23:30:33 
mathjax: true 
categories: [算法, LeetCode]
tags: [算法, LeetCode]
---

## 1.整数反转

给你一个 32 位的有符号整数 x ，返回将 x 中的数字部分反转后的结果。  
如果反转后整数超过 32 位的有符号整数的范围[?231, 231 ? 1]，就返回 0。  
假设环境不允许存储 64 位整数（有符号或无符号）。

### 示例

#### 示例1

> **输入**: x = 123  
> **输出**: 321

#### 示例2

> **输入**: x = -123  
> **输出**: -321

#### 示例3

> **输入**: x = 120  
> **输出**: 21

#### 示例4

> **输入**: x = 0  
> **输出**: 0

### 提示

* $-2^{31} \le x \le 2^{31}$

<!-- more -->

### 思路及算法

我们可以一次构建反转整数的一位数字。反转整数的方法可以与反转字符串进行类比。  
我们想重复“弹出”x的最后一位数字，并将它“推入”到result的后面。最后，result将与x相反。  
要在没有辅助堆栈/数组的帮助下“弹出”和“推入”数字，我们可以使用数学方法。

#### 代码

```kotlin
class Solution {
    fun reverse(x: Int): Int {
        var result = 0L
        var tmp = x
        while (tmp != 0) {
            result = result * 10 + tmp % 10
            tmp /= 10
        }
        return result.toInt().takeIf { it.toLong() == result } ?: 0
    }
}
```

### 复杂度分析

* 时间复杂度: $O(lg(x))$，$x$中大约有$lg(x)$位数字
* 空间复杂度: $O(1)$
